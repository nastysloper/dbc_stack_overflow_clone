<%= form_for Comment.new(parent: @comment, event: @event), html: {class: "hidden"} do |f| %>
  <%= f.hidden_field :parent_id %>

  <%= f.hidden_field :event_id %>
  
  <%= f.text_area :text, size: "50x4" %><br>
  <%= f.submit "Reply" %>
<% end %>