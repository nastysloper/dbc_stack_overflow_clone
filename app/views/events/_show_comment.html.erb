<div class="comments" data-id="<%= @comment.id %>">
  <% if current_user %>
    <% vote = @comment.vote_by(current_user) %>
  <% end %>
  
  <% if vote %>
    <div><%= @comment.votes_sum %> voted!</div>
    <a class="un-vote" href="#" data-id="<%= vote.id %>">un-vote</a>
  <% else %>
    <div><%= @comment.votes_sum %></div>
    <a class="up-vote" href="#">Up!</a>
    <a class="down-vote" href="#">Down ;(</a><br>
  <% end %>

  <div><%= @comment.text %></div>
  <div>Posted by: <%= @comment.author.twitter_handle %></div><br>

  <a class="reply" href="#">reply</a>
  <div class="reply" id="reply<%= @comment.id %>">
    <%= render "comment_form" %>
  </div>

  <hr>

  <div class="nested">
    <% @comment.replies.each do |comment| %>
      <% @comment = comment %>
      <%= render "show_comment" %>
    <% end %>
  </div>
</div>